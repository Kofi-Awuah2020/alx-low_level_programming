#include "main.h"

/**
 * add - adds two integers and returns the result.
 * @x: used in addition
 * @y: used in addition
 *
 * Return: 0 (Success)
 */
int add(int x, int y)
{
	int result;

	result = x + y;
	return (result);
}
